/*using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace TileMapGameEngine
{
    internal class publicInterface
    {

        Tile myTile = new Tile();

        TileMap map = new TileMap(Tile);

        Knight


    }

    enum Direction
    {s
        Up, Down, Left, Right
    }


    public class Knight : TileObject
    {
        public Knight(Position pos, IActor owner)
        {
            SetStartingPosition(pos);
            Owner = owner;
        }

        SetStartPosition(Positioning_TileMapEngine pos);

        AddNewMove(Direction.Up, Direction.Up, Direction.Left);
        AddNewMove(Direction.Up, Direction.Up, Direction.Right);
        // etc...

        void OnTileObjectLand(TileObject other)
        {

        }
    }
}
*/